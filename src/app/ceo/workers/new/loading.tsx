import { WorkerRecordRouteLoading } from "@/components/operations/route-loading";

export default function Loading() {
  return (
    <WorkerRecordRouteLoading
      backLabel="Back to workers"
      title="Create worker"
    />
  );
}
